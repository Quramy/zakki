# 4/7

## ハーネスエンジニアリング

最近よく聞くようになった単語。AI エージェントに対するハーネス(harness; 手綱) という意味。

> ハーネスエンジニアリングは、AI コーディングエージェントを“自由に走らせる”のではなく、守るべき制約を明示し、逸脱したら検知・修正・停止できるようにする考え方です。名前のとおり、「手綱（harness）」のメタファーに近いものです。

https://developers.gmo.jp/technology/81389/ より

特定の機能や仕組みというよりは、ガードレールと同じような意味合いで、以下を組み合わせて実現する概念といったイメージ。

- CLAUDE.md
- .claude/rules
- hooks
- CI

## Vim と Biome とか oxfmt とか

Vite Plus(oxfmt) やら Biome やらで Prettier ライクなフォーマッタが色々出てきていて、作業中のプロジェクトがどのフォーマッタを利用しているかが様々。

一方で自分の Vim だと fork した vim-prettier を利用し続けているが、こいつは Biome や oxfmt に対応していないのでそろそろ辛い。
vim-prettier のコードを軽く読んだ(Claude に読ませた) ところ、 `:system` で Prettier の CLI に標準入力でバッファの内容渡している程度なので、拡張すれば他フォーマッタのバイナリ使わせるのもそう難しくはなさそう。

`./node_modules/.bin` に複数のフォーマッタが見つかった場合の優先度をどうするかがちょっと悩みどころか。

- Prettier より Biome, oxfmt が優先
- oxfmt より Vite Plus が優先
- Vite Plus より Biome が優先

こんな感じか？

## ts-graphql-plugin の TypeScript v6 対応

CI のどこで失敗しているのかと思ったら Dev Dependencies に入れてた talt が peer で TypeScript v5.x に縛っていた。
まずはこっちから v6 対応させていく。そして久々の Publish なので Legacy Token 剥がしの作業から。。。

---

# 4/8

## `npm install -g npm` で MODULE_NOT_FOUND エラー

ts-graphql-plugin の CI で trusted publishing の都合で npm を最新にしようとしたら、以下のエラーが出て失敗してしまう。

```
  npm install -g npm@latest
  shell: /usr/bin/bash -e {0}
npm error code MODULE_NOT_FOUND
npm error Cannot find module 'promise-retry'
npm error Require stack:
npm error - /opt/hostedtoolcache/node/22.22.2/x64/lib/node_modules/npm/node_modules/@npmcli/arborist/lib/arborist/rebuild.js
npm error - /opt/hostedtoolcache/node/22.22.2/x64/lib/node_modules/npm/node_modules/@npmcli/arborist/lib/arborist/index.js
npm error - /opt/hostedtoolcache/node/22.22.2/x64/lib/node_modules/npm/node_modules/@npmcli/arborist/lib/index.js
npm error - /opt/hostedtoolcache/node/22.22.2/x64/lib/node_modules/npm/node_modules/libnpmfund/lib/index.js
npm error - /opt/hostedtoolcache/node/22.22.2/x64/lib/node_modules/npm/lib/utils/reify-output.js
npm error - /opt/hostedtoolcache/node/22.22.2/x64/lib/node_modules/npm/lib/utils/reify-finish.js
```

Setup Node action で CI の Node.js を 24 系まで上げて workflow から `npm install` を削除して事なきを得たが、なんだったんだ...?
